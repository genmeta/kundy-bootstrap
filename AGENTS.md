# immo 项目协作规则

## 注释规范

- 只为安全边界、业务不变量、事务行为、外部系统约束或不明显的取舍添加注释。
- 不要为一看就懂的 CRUD 处理器、模型字段、简单校验、赋值语句或直接的 SQL 行映射添加注释。
- 注释应简短，解释代码为什么存在，而不是逐行翻译下一行代码。
- 注释有必要时，先写中文，再用只含注释符号的空注释行分隔，最后写对应的英文。
- Rust（`//`）和 SQL（`--`）使用相同的注释规则。

## 模块规范

- 模块根文件使用 `<模块名>.rs` 风格，不使用 `mod.rs`。
- 启动前必须初始化的环境、日志、连接、迁移和后台任务放在 `init.rs`；`main.rs` 只负责启动服务。
